<%= render "admin/shared/message" %>
<div class="row">
  <div class="col-md-12">
    <div class="col-md-6">
      <h2>Courses</h2>
    </div>
    <div class="col-md-6">
      <div class="model_buttons">
        <%= link_to "Add New", new_admin_course_path, class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
</div>
<div class="model_line"></div>
<div class="row">
  <div class="col-md-12">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Course Name</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% if @courses.present? %>
          <% @courses.each do |course| %>
            <tr>
              <td>
                <%= course.name %>
              </td>
              <td>
                <%= link_to (fa_icon "edit").html_safe,edit_admin_course_path(course.id),{title:'Edit',class: 'btn-sm btn-primary'} %>
                <%= link_to (fa_icon "trash").html_safe,admin_course_path(course),{title:'Delete',class: 'btn-sm btn-danger',method: :delete,data: {confirm: "Are you sure you want to delete this?"}} %>
              </td>
            </tr>
          <% end %>
        <% else %>
          <tr>
            <td class="center" colspan="6">No Record Found</td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
